# Slither security-gate source boundary

This feature owns exact-tool execution, finding policy and sanitised evidence.
It is static analysis rather than an audit. Production code follows `domain <-
application <- adapters <- composition`; workflow wiring remains integrator
owned.
